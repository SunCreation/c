#include <stdio.h>

int main(void) {
    // char str[] = "coding";
    // printf("%s, %ld\n", str, sizeof(str));
    // for (int i =0; i< sizeof(str); i++) {
    //     printf("%c\n", str[i]);
    // }
    // char kor[] = "나도코딩"; // utf-8에서 공간 3씩 차지
    // printf("%s, %ld\n", kor, sizeof(kor));
    // char c_array[7] = "ha..";
    // // printf("%s\n", c_array);
    // for (int i = 0; i < sizeof(c_array); i++) {
    //     printf("%c\n", c_array[i]);
    // }
    char * hhh;
    scanf("%s", hhh);
    printf("%s", hhh);

}